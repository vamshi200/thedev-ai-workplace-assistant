const technicalData = {
    "npm install failed":
      "Try: 1. Delete node_modules 2. Run npm install 3. Check package.json 4. Restart npm run dev",
  
    "react app not running":
      "Check: 1. npm install 2. npm run dev 3. Port conflicts 4. Browser console errors",
  
    "git merge conflict":
      "Steps: 1. git status 2. Open conflict files 3. Resolve markers 4. git add . 5. git commit",
  
    "module not found":
      "Possible causes: 1. Wrong import path 2. Missing dependency 3. File name mismatch 4. Run npm install",
  
    "api not working":
      "Check: 1. Endpoint URL 2. Request payload 3. Network tab 4. Backend logs",
  
    "docker issue":
      "Try: 1. docker ps 2. docker logs container 3. Restart container 4. Verify ports",
  
    "python environment":
      "Steps: 1. python -m venv venv 2. Activate virtual environment 3. pip install -r requirements.txt 4. Verify Python version",
  
    "api failing":
      "Check endpoint URL, authentication token, request body, response status code, backend logs and CORS settings.",
  
    "database connection":
      "Verify credentials, host, port, firewall rules, connection string, database name and active connection pool.",
  
    "node modules issue":
      "Delete node_modules and package-lock.json, then run npm install again.",
  
    "deployment failed":
      "Check build logs, environment variables, package scripts, deployment configuration and server runtime version.",
  
    "code review":
      "Review checklist: 1. Error handling 2. Input validation 3. Security 4. Logging 5. Performance 6. Code readability",
  
    "react error":
      "Check console error, component imports, props, state updates, hooks usage and missing dependencies.",
  
    "frontend issue":
      "Check browser console, inspect network requests, validate CSS/layout, restart dev server and verify package imports.",
  
    "backend issue":
      "Check API logs, database connectivity, authentication, request payload, environment variables and server health."
  };
  
  export default technicalData;